MONGODB_URI="mongodb://localhost:27017/print-service"
PORT=6000
PUBLIC_BASE_URL="https://print-service.example.com"

# Base URL of Prime's API — AuthGuard validates every /api/printers request's
# bearer token against <PRIME_API_URL>/users/authorities/v2 (mirrors prime-dr3).
PRIME_API_URL="https://api.prime.example.com"